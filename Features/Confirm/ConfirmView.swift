import SwiftUI

struct ConfirmView: View {
    var body: some View {
        NavigationStack {
            Text("Confirm")
                .navigationTitle("확인")
        }
    }
}

#Preview {
    ConfirmView()
}
